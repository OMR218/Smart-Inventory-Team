apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: marwan774/product-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4002
          livenessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 10
            periodSeconds: 5
          env:
            - name: PORT
              value: "4002"
            - name: MONGO_URI
              value: mongodb://mongo:27017/product_db
            - name: JWT_SECRET
              value: "smart-inventory-secret"
            - name: REDIS_URL
              value: "redis://redis:6379"
            - name: RABBITMQ_URL
              value: "amqp://rabbitmq:5672"
